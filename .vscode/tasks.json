{
    "tasks": [
        {
            "type": "shell",
            "label": "build project",
            "command": "/usr/bin/make",
            "args": [
                "gdb"
            ],
            "options": {
                "cwd": "${workspaceFolder}/Code"
            }
        }
    ],
    "version": "2.0.0"
}